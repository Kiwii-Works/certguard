# CertGuard

CertGuard is an internal administrative application used to manage certificates, track expiration dates, import certificate data from Excel files, and send configurable email notifications.

## Tech Stack

- FastAPI
- Python 3.13
- SQLite (dev)
- JWT Authentication

## Setup

```bash
cd backend
pip install -r requirements.txt